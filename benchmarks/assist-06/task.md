# Task assist-06: scaffold an experiment from the template

Create `experiments/pallet_extract/` from the webapp template described in
`TEMPLATE.md` (README, run.py with `--port`, requirements.txt, .env.example)
adapted to "extract pallet ids from uploaded text files".

## Acceptance criteria
- All four files exist under experiments/pallet_extract/
- run.py accepts `--port` and mentions pallet extraction
- README explains how to run it
