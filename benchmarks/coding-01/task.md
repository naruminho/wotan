# Task coding-01: add a power feature with tests

The calculator module `calc.py` supports add and multiply. Add `power(a, b)`
(integer power, `power(a, 0) == 1`) **with tests** in `test_calc.py`.

## Acceptance criteria
- `calc.power` exists and is correct (incl. negative exponents -> float)
- New tests added and the whole suite passes
- No existing test changed or removed
