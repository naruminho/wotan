# Task review-01: find the injected bug

`basket.py` has one subtle bug: `test_basket.py` fails on
`apply_voucher("SUMMER10")`. Find and fix the bug (not the test).

## Acceptance criteria
- Tests pass unmodified
