# Task assist-02: extract entities to CSV (no LLM available here)

From `notes/`, build `entities.csv` with columns `file,text,type` listing the
people, organizations and money amounts mentioned (type = person /
organization / money). Deterministic extraction only.

## Acceptance criteria
- `entities.csv` exists with correct columns
- All 5 entities from the notes are listed with correct types
