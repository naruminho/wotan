# Task debug-05: off-by-one pagination

`paginate.py` returns wrong pages (failing tests show the expected
0-based, `size`-length windows). Fix the code.

## Acceptance criteria
- Tests pass unmodified
