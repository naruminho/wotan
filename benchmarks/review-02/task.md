# Task review-02: review a risky change

Read `PR.md` and `worker.py`. Write `review.md` with at least **3** concrete
findings (at least one each about error handling, concurrency/race, and
rollback/timeout). Point to exact lines and suggest fixes.

## Acceptance criteria
- `review.md` exists with >= 3 findings
- Covers error handling, race/concurrency, and rollback/timeout concerns
