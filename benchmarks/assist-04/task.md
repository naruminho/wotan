# Task assist-04: draft a document template set

Create `templates/README.template.md` and `templates/CHANGELOG.template.md`
for a new microservice named in `brief.md`, with standard sections
(Overview, Endpoints, Configuration / Unreleased, Added, Fixed).

## Acceptance criteria
- Both templates exist with the required sections
- The service name from brief.md appears in the README template
