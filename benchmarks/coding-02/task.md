# Task coding-02: implement the missing module against given tests

`stats.py` is missing. Implement `mean(values)` and `median(values)`
(median of even count = average of the two middle values) so the provided
tests pass. Do not modify the tests.

## Acceptance criteria
- All tests pass unmodified
- Empty input raises ValueError
