# Task assist-03: build a report from log files

From `logs/`, write `report.md` with: total requests, error count, the
top endpoint by calls, and 3 recommended actions. Numbers must be exact.

## Acceptance criteria
- report.md states 8 total requests and 2 errors
- Names the top endpoint (`/api/shipments`)
- Lists 3 recommendations
