# Copy to .env. Values here OVERRIDE nothing: wotan reads its own config at
# ~/.wotan/config.yaml and resolves credentials from env/keyring.
GATEWAY_URL=http://127.0.0.1:8787
GATEWAY_CLIENT_ID=demo-client
GATEWAY_CLIENT_SECRET=change-me
